#if( $#ARGV < 1 ) {
#	print "Usage : $0 SisIYA_client_conf.pm expire [output_file]\n";
#	print "The expire parameter must be given in minutes.\n";
#	exit 1;
#}

#my $conf_file = $ARGV[0];
#my $expire = $ARGV[1];
#my $output_file = '';
#if($#ARGV == 3) {
#	$output_file = $ARGV[2];
#	if( ! -e $output_file) {
#		die "$0 : File $output_file does not exist!";
#	}
#}


